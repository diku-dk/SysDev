from Model.Teacher import Teacher


class CourseResponsible(Teacher):
    """
    a course responsible is a special type of teacher,
    that on top of the responsibilities of a teacher, can request for course changes
    """


